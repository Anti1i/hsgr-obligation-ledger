# Benchmark decision (2026-08-10 evening, REVISED after controls job 724624)
# Model: Qwen2.5-7B-Instruct
#
# GSM8K-deep (socratic E0, n=300):
#   SC@1=0.900  oracle-hops=0.787  tax=+0.113  -> no headroom
#
# MuSiQue ans-dev open-book gold supports (n=200):
#   structure: depth=2.65  reconvergence=14.5%
#   SC@1=0.448  SC@3=0.470 (budget-matched)  SC@8=0.500  oracle@8=0.685
#
# E0 gold-decomp, FIRST PASS (evidence = per-hop gold paragraph):
#   predicted=0.535  oracle=0.640   predicted-SC@1=+0.095
#   ^ RETRACTED as evidence for structure-as-control. Two confounds, both inflating:
#     (1) each hop was handed its own gold support paragraph = paragraph-level
#         retrieval supervision, while SC had to locate facts across all supports;
#     (2) no token accounting. Hops re-send evidence per hop; measured cost is
#         97% prefill, 2.61 prefills/problem, so the matched baseline is SC@3.
#
# CONTROLS (job 724624):
#   evidence=all (matched evidence):  predicted=0.520  oracle=0.555
#     oracle loses 8.5pp once the paragraph mapping is removed.
#   vs SC@8:  predicted +0.020 p=0.66 ns ;  oracle +0.055 p=0.14 ns
#   vs SC@3 (budget-matched):  predicted +0.050 (not significant at n=200)
#   oracle vs oracle@8:  -0.130  p=0.0002  SIG  -> structure does NOT expand coverage
#   by hop count (evidence=all, predicted - SC@1):
#     2hop n=110 +10.0pp | 3hop n=58 +8.6pp | 4hop n=32 +0.0pp
#     ^ gain DECAYS with depth, opposite of the structure-as-control prediction.
#       The entire 4-hop advantage was the gold paragraph mapping.
#
# DECISION: MuSiQue stays the benchmark, but the structure-as-control hypothesis
#   fails its own controls. Benchmark was never the problem: GSM8K rejects it via
#   a +11pp decomposition tax, MuSiQue via a non-significant, depth-decaying gain.
#
# E1 (learn decomposition/dependency edges): CANCELLED. The quantity it would
#   learn has no surviving gain, and a learned structure carries no gold paragraph
#   mapping, so it cannot capture the one large real effect.
# E2 (decomposition-level candidate domain): ON HOLD, same reason.
#
# E2' LATENT VERIFIER (job 724632, H100) — GATE FAIL
#   Setup: verify-prompt last-token hiddens, 5-fold OOF logistic+rank probe, argmax.
#   best layer=14  OOF wp-AUROC=0.730 (n=88 mixed)  pooled AUROC=0.770
#   SC@1=0.440  SC@8=0.500  probe=0.525  oracle@8=0.685
#   delta vs SC@8 = +0.025 (closes 13.5% of 18.5pp gap; need >=33%)
#   McNemar p=0.499  GATE FAIL
#
# E2' PROBE-WEIGHTED VOTE (job 724678, CPU) — GATE FAIL
#   score(answer) = vote_fraction + lambda * mean within-problem z(probe)
#   lambda selected out-of-fold: 0.2 / 0.3 / 0.3 / 0.2 / 0.2
#   SC@8=0.500  combined=0.530  delta=+0.030
#   paired changes: combined-only=14  SC@8-only=8  McNemar p=0.286
#   preregistered gate: >=+0.060 AND p<0.05  -> FAIL
#   Ranking signal is readable; neither argmax nor weighted voting establishes
#   a selection gain. The cheap linear latent-probe line stops here.
#
# HSGR DEPENDENCY-USE LATENT GUIDE (job 724723, H100) — GATE FAIL
#   40 calibration / 160 held-out; selected feature layer 21, beta=0.20.
#   held-out: base=0.519  text-guide=0.488  latent=0.494  anti=0.494
#   latent vs base: -0.025, latent-only=4 base-only=8, exact p=0.388
#   latent vs anti: 0.000, each-only=12, exact p=1.000
#   A fixed instruction-contrast direction has no dependency-specific causal
#   effect. A true online structural-residual reader/controller remains untested.
#   Do not run more steering unless a natural pre-answer dependency-compliance
#   reader first passes a held-out AUROC >=0.75 gate.
#
# NEXT: stop-loss on fixed steering and cheap latent reranking. Options:
#   (a) diagnostic only: a natural pre-answer dependency-compliance reader;
#       require held-out AUROC >=0.75 before any online residual steering;
#   (b) stronger verifier / larger n — cost up and claim shifts;
#   (c) rewrite around the falsifications, V3 readability, and the unresolved
#       selection gap; do not claim that HSGR Guide is already effective.

# FINQA PROGRAM-INTERVENTION STAGE A (job 728892, commit 9e96ad0) — GATE FAIL
#   Independent gold parse/execution: 100% on train/dev/public test.
#   Public test n=1147: 1-step=654, 2-step=409, deep(depth>=3,length>=3)=71,
#   two-reference join=33, deep+join=21, branching=8.
#   Frozen structural gates: deep>=150 FAIL; join>=100 FAIL.
#   Decision: FinQA is valid for short atomic program repair but is too shallow
#   and linear to validate a hierarchy-conditioned HSGR Guide. Do not run the
#   predicted-program, edit-oracle, GPU, or hidden-state stages on FinQA under
#   this protocol. Full result: EXPERIMENT_FINQA_STRUCTURE_AUDIT_728892.md.

# EXECUTABLE-DAG BENCHMARK AUDIT P0 (job 728924, commit 1a1a7d4)
#   Frozen operation-dependency graph definition; zero model / zero GPU.
#   MultiHiertt dev: programs=842, deep=187, join=18, reuse=0,
#     deep+join=16, deep+join+reuse=0 -> STRUCTURE FAIL.
#   HiTab dev: programs=1671, parse=12.5%, deep=14, join=4, reuse=0 -> FAIL.
#   MathQA test: programs=2985, parse=100%, deep=2083, join=1576, reuse=535,
#     deep+join=1386, deep+join+reuse=535, diamond=108 -> STRUCTURE PASS.
#   Full result: EXPERIMENT_EXECUTABLE_DAG_BENCHMARK_AUDIT_728924.md.

# MATHQA EXECUTOR AUDIT P0-B (job 728934, commit ce9333c) — GATE FAIL
#   Root-connected deep+join+reuse targets: train=5321, test=535.
#   Execution coverage: train=98.65%, test=97.76% (both pass).
#   Gold-option agreement among executed: train=61.50%, test=59.27%; frozen
#     requirement >=95% -> both FAIL. Unsupported ops are too rare to explain it;
#     target-only inspection finds both clear program inconsistencies and some
#     rounding near-misses under the fixed 1% tolerance.
#   Although 3228 train / 310 test targets execute and match, treating this
#     outcome-filtered subset as a PASS would be post-hoc. Under the frozen stop
#     rule do not run Qwen, atomic-edit, GPU, or hidden-state stages on MathQA.
#   Full result: EXPERIMENT_MATHQA_EXECUTOR_AUDIT_728934.md.
#
# CURRENT BENCHMARK VERDICT: none of FinQA, MultiHiertt, MathQA, or HiTab passes
#   both the structural and executable-gold requirements under a pre-registered
#   native-data protocol. Stop hopping standard numerical-reasoning benchmarks.

# DEPENDENCY-ROLE ACTIVATION PATCHING P0 (job 731009, commit cceab9f) —
#   REPRESENTATION FAIL; TASK/APPARATUS FAIL
#   Frozen controlled programs: 96 calibration / 192 held-out, Qwen2.5-7B,
#   candidate residual layers 7/14/21. Selected layer=21.
#   Held-out P-vs-matched-X: paired acc=0.552, AUROC=0.561, sign p=0.085;
#   fixed halves=0.644/0.476; metadata=0.490; representation gate FAIL.
#   Clean executable accuracy=0.099; corrupt-own accuracy=0.068, so the model
#   did not perform the direct-answer base task. Corruption nevertheless lowered
#   clean-answer logp by 0.262 nats, CI [0.089, 0.436]. Root-positive patches
#   changed logp by +2.27 to +2.41 nats, confirming hook sensitivity, but did
#   not rescue accuracy. No representation-utilization-gap claim is allowed.
#   NEXT: disjoint apparatus-only screen of simpler topology-matched programs;
#   select only by task accuracy/corruption sensitivity, then use a new seed for
#   any P1 probe/causal test. Full result: EXPERIMENT_DEPENDENCY_PATCH_P0_731009.md.

# DEPENDENCY APPARATUS-ONLY SCREEN (job 731104, commit 5050b69) — PASS AT
#   ONE-HOP ONLY
#   Fixed hardest-to-easiest families, n=96 each; no hidden states or patches.
#   dag_add clean/corrupt-own=0.063/0.104 FAIL; chain3_add=0.167/0.094 FAIL;
#   chain3_copy=0.990/0.458 FAIL; chain1_copy=1.000/0.667 PASS.
#   Three-step identity succeeds when P==X but fails reliable print-route
#   following after P differs from decoy X. Arithmetic is not the sole issue;
#   dependency routing degrades with depth. Frozen selection is chain1_copy.
#   P1 may test only the minimal one-hop dependency-role mechanism on new seed;
#   any pass is not a hierarchy claim. Full result:
#   EXPERIMENT_DEPENDENCY_APPARATUS_SCREEN_P1_731104.md.

# ONE-HOP DEPENDENCY-ROLE ACTIVATION PATCHING P1 (job 731174, commit 67c04ac)
#   — FROZEN CAUSAL PASS, NARROW CLAIM ONLY
#   New seed; 96 calibration / 192 held-out chain1_copy cases; layers 7/14/21.
#   Representation selected layer 21 on calibration. Held-out P-vs-X paired
#   acc=0.714, AUROC=0.697, sign p=1.46e-9; fixed halves=0.693/0.736;
#   metadata=0.510, so the frozen representation gate passes.
#   Task is valid: clean acc=1.000, corrupt-own acc=0.734; corruption lowers
#   clean-answer logp by 10.633 nats, CI [9.649, 11.628].
#   At layer 21, correct-role vs equal-valued wrong-route patch:
#   logp delta=+0.262 nats, CI [0.150, 0.396]; accuracy delta=+7.81pp;
#   hash halves=+0.377/+0.134; recovery=99.84%. Frozen verdict CAUSAL_PASS.
#   This supports a minimal one-hop dependency-role mechanism, not hierarchy.
#   Remaining confound: correct donor is from the receiver's same token position,
#   while wrong-route donor is from another position. NEXT: freeze a same-position
#   print-route swap control before any depth progression or HSGR Guide claim.
#   Full result: EXPERIMENT_DEPENDENCY_PATCH_P1_731174.md.
