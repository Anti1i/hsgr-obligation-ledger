# Two-channel transition consensus (job 726439)

## Status

**DO NOT CONSUME FINAL HOLDOUT.** The two component readers reproduced their prior results exactly, but development OOF selected `alpha=0`, so the relation channel was rejected before held-out scoring.

The paired-only arm remained at 50.9375%, the bilinear-only arm at 50.6250%, and the selected consensus exactly equaled paired-only. All positive `alpha` values tied or underperformed the 61.0% development OOF maximum reached at `alpha=0`; the frozen tie rule preferred the smaller alpha.

This closes the cheap structural-reader composition route. The remaining technical option is a stronger outcome-supervised, Guide-conditioned listwise verifier trained on all previously consumed feature sets, with an equal-capacity non-hidden control and route-swap/mismatch tests. The final 377 untouched problems remain sealed.

